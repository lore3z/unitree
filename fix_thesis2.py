import re

with open('thesis/main.tex', 'r', encoding='utf-8') as f:
    text = f.read()

# Fix the instances with "17de59a" and "bd00c6a"
text = re.sub(
    r'其实和代码提交顺序是对应的：最早在 17de59a 把 G1 任务骨架搭起来时，主要目标还是先让机器人学会“能走”；到了 bd00c6a 对 g1、h1.*?的训练逻辑做统一调整后，动作平滑、关节限幅和姿态约束才真正开始压住那些看起来能拿分、但实际上不太能落地的动作；再往后做 push robot 相关修改时',
    r'在项目初期搭建基础任务骨架时，主要目标是先让机器人学会“能走”；而在后续对不同机器人平台的训练逻辑进行统一重构后，动作平滑、关节限幅和姿态约束等项才真正开始发挥核心作用，抑制了那些虽然能获取高分但难以在现实中落地的动作；再往后引入机器人受力扰动（Push Robot）的训练模块时',
    text,
    flags=re.DOTALL
)

text = re.sub(
    r'最早的 17de59a 先把 G1 任务和基础配置搭起来，重点是让训练链路先跑通；到了 bd00c6a，g1、h1.*?的训练逻辑开始一起调整',
    r'在项目早期的原型版本中，首先搭建了 G1 的任务及基础配置，重点是为了打通训练链路；随后进入架构重构阶段，对多平台的训练逻辑进行了统一调整',
    text,
    flags=re.DOTALL
)

text = re.sub(
    r'从版本迭代上看，代码历史之所以有意义，是因为它记录了这些问题是怎么一步步被发现的。17de59a 时先搭任务骨架，说明当时最紧要的是把环境和训练链路接上；bd00c6a 开始统一 g1、h1.*?的训练逻辑，说明那一阶段已经在做跨任务的参数收敛；到了后面和 push robot 相关的修改阶段',
    r'从迭代历程上看，工程记录展示了问题逐步暴露与解决的脉络。在初步构建阶段搭建任务骨架，重点在于走通仿真与训练的闭环流程；在架构统一阶段整合多个平台的训练基类，表明研究焦点已转向跨环境的参数一致性收敛；而到了后期加入外部扰动（如施加推力）的阶段',
    text,
    flags=re.DOTALL
)

with open('thesis/main.tex', 'w', encoding='utf-8') as f:
    f.write(text)
